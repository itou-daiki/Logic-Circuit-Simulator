# ロジックサーキットシミュレータ

高校生向けの論理回路学習ツール

## プロジェクト概要

このプロジェクトは、高校生が論理回路（AND、OR、NOT回路）を視覚的に学べるWebベースのシミュレータです。ドラッグ&ドロップで回路を配置し、マウスで接続して、実際の動作をステップ実行で確認できます。

## 機能仕様

### 基本機能
- **回路コンポーネント配置**: AND、OR、NOT回路をマウスドラッグで自由配置
- **回路接続**: マウスクリック&ドラッグで回路間を接続線で結合
- **シミュレーション**: 配置完了後にシミュレートボタンでロジック動作を開始
- **ステップ実行**: 進む/戻るボタンで信号の流れを段階的に確認
- **信号表示**: 各接続点での信号状態（0/1）をリアルタイム表示

### 教育的配慮
- 直感的なユーザーインターフェース
- 回路動作の視覚的フィードバック
- エラー防止機能（不正な接続の検出）
- 学習ガイド機能

## 技術仕様

### フロントエンド
- **HTML5**: セマンティックマークアップ
- **CSS3**: レスポンシブデザイン、アニメーション効果
- **JavaScript (ES6+)**: 
  - Canvas APIによる回路描画
  - ドラッグ&ドロップAPI
  - イベント処理
  - 論理演算処理

### デプロイメント
- **GitHub Pages**: 静的サイトホスティング
- **単一HTMLファイル**: 依存関係なしで完全動作

## ファイル構成

```
logic-circuit-simulator/
├── index.html              # メインアプリケーションファイル
├── README.md              # プロジェクト説明書
├── docs/
│   ├── user-guide.md      # ユーザーガイド
│   └── technical-spec.md  # 技術仕様書
└── assets/
    └── screenshots/       # スクリーンショット画像
```

## 開発ロードマップ

### Phase 1: 基本実装
1. **キャンバス環境構築**
   - HTML5 Canvasの初期化
   - 座標系とグリッドシステム
   - 基本描画機能

2. **回路コンポーネント**
   - AND、OR、NOT回路クラス設計
   - コンポーネント描画機能
   - 入出力ピン管理

3. **ドラッグ&ドロップ**
   - マウスイベント処理
   - コンポーネント選択・移動
   - 配置位置の制約

### Phase 2: 接続機能
1. **接続線描画**
   - ベジェ曲線による滑らかな線
   - 接続点の自動検出
   - 視覚的フィードバック

2. **接続管理**
   - 接続データ構造
   - 循環参照検出
   - 接続の妥当性チェック

### Phase 3: シミュレーション
1. **論理演算エンジン**
   - 真理値計算
   - 信号伝播アルゴリズム
   - 遅延シミュレーション

2. **ステップ実行**
   - 実行状態管理
   - アニメーション制御
   - デバッグ情報表示

### Phase 4: UI/UX改良
1. **インターフェース**
   - ツールパレット
   - プロパティパネル
   - ヘルプシステム

2. **教育機能**
   - チュートリアル
   - 演習問題
   - 回路保存・読込

## 実装の技術的詳細

### 回路コンポーネントクラス設計

```javascript
class LogicGate {
    constructor(type, x, y) {
        this.type = type; // 'AND', 'OR', 'NOT'
        this.x = x;
        this.y = y;
        this.inputs = [];
        this.outputs = [];
        this.value = null;
    }
    
    calculate() {
        // 論理演算実装
    }
    
    draw(ctx) {
        // Canvas描画実装
    }
}
```

### 接続管理システム

```javascript
class Connection {
    constructor(fromGate, fromPin, toGate, toPin) {
        this.from = { gate: fromGate, pin: fromPin };
        this.to = { gate: toGate, pin: toPin };
        this.signal = 0;
    }
    
    propagateSignal() {
        // 信号伝播処理
    }
}
```

### シミュレーションエンジン

```javascript
class Simulator {
    constructor(gates, connections) {
        this.gates = gates;
        this.connections = connections;
        this.currentStep = 0;
        this.history = [];
    }
    
    stepForward() {
        // 1ステップ進める
    }
    
    stepBackward() {
        // 1ステップ戻る
    }
}
```

## ユーザーエクスペリエンス設計

### 操作フロー
1. **回路選択**: ツールパレットから回路タイプを選択
2. **配置**: キャンバス上でクリックして配置
3. **接続**: 出力ピンから入力ピンへドラッグして接続
4. **入力設定**: 入力ゲートの値を設定（0または1）
5. **シミュレート**: シミュレートボタンをクリック
6. **ステップ実行**: 進む/戻るボタンで動作を確認

### 視覚的フィードバック
- **ホバー効果**: マウスオーバー時のハイライト
- **選択状態**: 選択中コンポーネントの強調表示
- **信号状態**: 接続線の色変化（0:グレー、1:赤）
- **アニメーション**: 信号の伝播を動的表示

## 教育的価値

### 学習目標
1. **論理回路の基本理解**
   - AND、OR、NOT演算の動作原理
   - 真理値表との対応関係
   - 組み合わせ論理の概念

2. **実践的スキル**
   - 回路設計の基本手順
   - デバッグとトラブルシューティング
   - 段階的問題解決

3. **発展的学習**
   - 複合回路の設計
   - 最適化の考え方
   - デジタル回路への応用

### 演習例
1. **基本演習**: 単一ゲートの動作確認
2. **組み合わせ演習**: 2段回路の設計
3. **応用演習**: 半加算器の実装
4. **発展演習**: デコーダ回路の設計

## 今後の拡張予定

### モバイル対応仕様

#### レスポンシブデザイン
- **スマートフォン縦画面対応**: 768px以下の画面サイズに最適化
- **タッチファースト設計**: モバイルデバイスでの操作を優先
- **フレキシブルレイアウト**: 画面サイズに応じた要素の再配置
- **スケーラブルUI**: コンテンツの拡縮に対応

#### タッチ操作最適化
- **タップターゲット拡大**: 最小44px×44pxのタッチエリア確保
- **誤タップ防止**: ボタン間隔の最適化とタッチガード実装
- **スワイプ・ピンチ対応**: 直感的なタッチジェスチャー
- **視覚的フィードバック**: タッチ時のハイライト効果

#### モバイル専用機能
- **デバイス回転対応**: 縦横画面の自動調整
- **パフォーマンス最適化**: モバイル環境でのスムーズな動作
- **バッテリー効率**: 電力消費を抑えた処理

### シミュレーション機能強化

#### 段階的信号表示
- **信号伝播アニメーション**: 各回路での信号の変化を視覚的に表示
- **ステップ詳細表示**: 各段階での論理演算結果の表示
- **信号タイミング**: 信号の遅延と伝播速度の制御
- **デバッグモード**: 詳細な内部状態の表示

#### 教育的シミュレーション
- **スローモーション実行**: 学習者が理解しやすい速度での実行
- **ハイライト機能**: 現在処理中の回路とピンの強調表示
- **説明テキスト**: 各段階での処理内容の説明
- **エラー検出**: 回路設計の問題点を指摘

### 追加機能候補
- **回路テンプレート**: よく使用される回路パターンの保存
- **エクスポート機能**: 回路図の画像出力
- **共有機能**: URLによる回路の共有
- **成績管理**: 学習進捗の追跡
- **多言語対応**: 英語インターフェース

### 高度な機能
- **タイミング図**: 時系列での信号変化表示
- **回路最適化**: 冗長な回路の簡素化提案
- **3D表示**: 立体的な回路表現
- **シーケンス回路**: フリップフロップ等の対応

---

このプロジェクトは、論理回路の基礎を楽しく学べる教育ツールとして、高校生の情報教育に貢献することを目指しています。