# ロジックサーキットシミュレータ

高校生向けの論理回路学習ツール

## プロジェクト概要

このプロジェクトは、高校生が論理回路（AND、OR、NOT回路）を視覚的に学べるWebベースのシミュレータです。ドラッグ&ドロップで回路を配置し、マウスで接続して、実際の動作をステップ実行で確認できます。

## 機能仕様

### 基本機能
- **回路コンポーネント配置**: AND、OR、NOT回路をマウスドラッグで自由配置
- **回路接続**: マウスクリック&ドラッグで回路間を接続線で結合
- **シミュレーション**: 配置完了後にシミュレートボタンでロジック動作を開始
- **ステップ実行**: 進む/戻るボタンで信号の流れを段階的に確認
- **信号表示**: 各接続点での信号状態（0/1）をリアルタイム表示

### 教育的配慮
- 直感的なユーザーインターフェース
- 回路動作の視覚的フィードバック
- エラー防止機能（不正な接続の検出）
- 学習ガイド機能

## 技術仕様

### フロントエンド
- **HTML5**: セマンティックマークアップ
- **CSS3**: レスポンシブデザイン、アニメーション効果
- **JavaScript (ES6+)**: 
  - Canvas APIによる回路描画
  - ドラッグ&ドロップAPI
  - イベント処理
  - 論理演算処理

### デプロイメント
- **GitHub Pages**: 静的サイトホスティング
- **単一HTMLファイル**: 依存関係なしで完全動作

## ファイル構成

```
logic-circuit-simulator/
├── index.html              # メインアプリケーションファイル
├── README.md              # プロジェクト説明書
├── docs/
│   ├── user-guide.md      # ユーザーガイド
│   └── technical-spec.md  # 技術仕様書
└── assets/
    └── screenshots/       # スクリーンショット画像
```

## 開発ロードマップ

### Phase 1: 基本実装
1. **キャンバス環境構築**
   - HTML5 Canvasの初期化
   - 座標系とグリッドシステム
   - 基本描画機能

2. **回路コンポーネント**
   - AND、OR、NOT回路クラス設計
   - コンポーネント描画機能
   - 入出力ピン管理

3. **ドラッグ&ドロップ**
   - マウスイベント処理
   - コンポーネント選択・移動
   - 配置位置の制約

### Phase 2: 接続機能
1. **接続線描画**
   - ベジェ曲線による滑らかな線
   - 接続点の自動検出
   - 視覚的フィードバック

2. **接続管理**
   - 接続データ構造
   - 循環参照検出
   - 接続の妥当性チェック

### Phase 3: シミュレーション
1. **論理演算エンジン**
   - 真理値計算
   - 信号伝播アルゴリズム
   - 遅延シミュレーション

2. **ステップ実行**
   - 実行状態管理
   - アニメーション制御
   - デバッグ情報表示

### Phase 4: UI/UX改良
1. **インターフェース**
   - ツールパレット
   - プロパティパネル
   - ヘルプシステム

2. **教育機能**
   - チュートリアル
   - 演習問題
   - 回路保存・読込

## 実装の技術的詳細

### 回路コンポーネントクラス設計

```javascript
class LogicGate {
    constructor(type, x, y) {
        this.type = type; // 'AND', 'OR', 'NOT'
        this.x = x;
        this.y = y;
        this.inputs = [];
        this.outputs = [];
        this.value = null;
    }
    
    calculate() {
        // 論理演算実装
    }
    
    draw(ctx) {
        // Canvas描画実装
    }
}
```

### 接続管理システム

```javascript
class Connection {
    constructor(fromGate, fromPin, toGate, toPin) {
        this.from = { gate: fromGate, pin: fromPin };
        this.to = { gate: toGate, pin: toPin };
        this.signal = 0;
    }
    
    propagateSignal() {
        // 信号伝播処理
    }
}
```

### シミュレーションエンジン

```javascript
class Simulator {
    constructor(gates, connections) {
        this.gates = gates;
        this.connections = connections;
        this.currentStep = 0;
        this.history = [];
    }
    
    stepForward() {
        // 1ステップ進める
    }
    
    stepBackward() {
        // 1ステップ戻る
    }
}
```

## ユーザーエクスペリエンス設計

### 操作フロー
1. **回路選択**: ツールパレットから回路タイプを選択
2. **配置**: キャンバス上でクリックして配置
3. **接続**: 出力ピンから入力ピンへドラッグして接続
4. **入力設定**: 入力ゲートの値を設定（0または1）
5. **シミュレート**: シミュレートボタンをクリック
6. **ステップ実行**: 進む/戻るボタンで動作を確認

### 視覚的フィードバック
- **ホバー効果**: マウスオーバー時のハイライト
- **選択状態**: 選択中コンポーネントの強調表示
- **信号状態**: 接続線の色変化（0:グレー、1:赤）
- **アニメーション**: 信号の伝播を動的表示

## 教育的価値

### 学習目標
1. **論理回路の基本理解**
   - AND、OR、NOT演算の動作原理
   - 真理値表との対応関係
   - 組み合わせ論理の概念

2. **実践的スキル**
   - 回路設計の基本手順
   - デバッグとトラブルシューティング
   - 段階的問題解決

3. **発展的学習**
   - 複合回路の設計
   - 最適化の考え方
   - デジタル回路への応用

### 演習例
1. **基本演習**: 単一ゲートの動作確認
2. **組み合わせ演習**: 2段回路の設計
3. **応用演習**: 半加算器の実装
4. **発展演習**: デコーダ回路の設計

## GitHub Pages デプロイ手順

### 初期設定
```bash
# リポジトリ作成
git init
git remote add origin https://github.com/username/logic-circuit-simulator.git

# 初回コミット
git add .
git commit -m "Initial commit: Logic Circuit Simulator"
git push -u origin main
```

### Pages有効化
1. GitHubリポジトリの「Settings」タブを開く
2. 「Pages」セクションで「Source」を「Deploy from a branch」に設定
3. 「Branch」を「main」、フォルダを「/ (root)」に設定
4. 「Save」をクリック

### アクセスURL
デプロイ後、以下のURLでアクセス可能：
`https://username.github.io/logic-circuit-simulator/`

## 開発環境セットアップ

### 推奨ツール
- **エディタ**: Visual Studio Code
- **ブラウザ**: Chrome/Firefox（開発者ツール使用）
- **バージョン管理**: Git

### 開発サーバー
ローカル開発には Live Server 拡張機能を推奨：
```bash
# VS Code拡張機能をインストール後
# index.htmlを右クリック → "Open with Live Server"
```

## 品質保証

### テスト項目
1. **機能テスト**
   - 各回路の論理演算正確性
   - 接続機能の動作確認
   - シミュレーション結果の妥当性

2. **ユーザビリティテスト**
   - 操作の直感性
   - エラーメッセージの適切性
   - レスポンス性能

3. **互換性テスト**
   - 主要ブラウザでの動作確認
   - モバイルデバイス対応
   - 画面サイズ適応

### パフォーマンス目標
- **初期読み込み**: 3秒以内
- **操作応答**: 100ms以内
- **シミュレーション実行**: 50ms以内

## ライセンス

MIT License - 教育目的での自由な利用・改変を許可

## 貢献ガイドライン

### Issue報告
バグ報告や機能要望は GitHub Issues で受付

### プルリクエスト
1. フォークしてブランチを作成
2. 変更を実装
3. テストを実行
4. プルリクエストを提出

## 今後の拡張予定

### 追加機能候補
- **回路テンプレート**: よく使用される回路パターンの保存
- **エクスポート機能**: 回路図の画像出力
- **共有機能**: URLによる回路の共有
- **成績管理**: 学習進捗の追跡
- **多言語対応**: 英語インターフェース

### 高度な機能
- **タイミング図**: 時系列での信号変化表示
- **回路最適化**: 冗長な回路の簡素化提案
- **3D表示**: 立体的な回路表現
- **シーケンス回路**: フリップフロップ等の対応

---

このプロジェクトは、論理回路の基礎を楽しく学べる教育ツールとして、高校生の情報教育に貢献することを目指しています。